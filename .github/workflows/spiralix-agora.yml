name: SPIRALIX Agora Validate
on:
  push:
    paths: ['config/spiralix-agora.json','tools/spiralix_agora.py','.github/workflows/spiralix-agora.yml']
  workflow_dispatch:
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python tools/spiralix_agora.py
