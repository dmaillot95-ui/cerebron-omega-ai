name: CEREBRON Collatz Paired Provider Sharding — PAUSED

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cerebron-collatz-paired-provider-sharding
  cancel-in-progress: false

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "PAIRED SHARDING IS PAUSED"
          echo "Reason: measured success rate fell sharply during paired-provider experiment."
          echo "Use baseline/adaptive benchmark before re-enabling any paired mode."
