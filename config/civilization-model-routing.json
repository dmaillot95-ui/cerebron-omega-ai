{
  "schema": "cerebron-civilization-model-routing-v1",
  "date": "2026-09-21",
  "status": "FROZEN_BEFORE_BINDING",
  "scope": {
    "colonies": 62,
    "model_bank": 6,
    "afah_lineage": "SEPARATE"
  },
  "routing": {
    "unit": "COLONY",
    "models_per_colony": 1,
    "shared_base_allowed": true,
    "shared_colony_state_forbidden": true,
    "simultaneous_colony_limit": 20,
    "rollout_batch": 2
  },
  "identity": [
    "canonical_colony_id",
    "selected_g0_model",
    "immutable_g0_revision",
    "colony_generation",
    "state_version",
    "parent_state_sha256",
    "content_sha256"
  ],
  "execution_order": [
    "resolve canonical colony",
    "verify permanent seed-farm links",
    "resolve approved G0 immutable revision",
    "verify compute compatibility receipt",
    "load latest monotonic HF-private colony head",
    "verify F66/F74 pointers and digest",
    "execute colony task",
    "audit and red-team",
    "write validated lesson only",
    "train only through separately gated reversible adapter pipeline"
  ],
  "anti_regression": [
    "never load state from another colony merely because base model is shared",
    "never replace newer generation with older state",
    "never treat GitHub cache as durable truth",
    "never auto-promote raw AGORA output",
    "never merge incompatible weights"
  ],
  "civilization_flow": "74 SEED FARMS -> 62 COLONIES -> CHIEFS -> AGORA -> CEREBRON -> AFAH",
  "current_wave": {
    "colonies": [
      "C03-SIGMA",
      "C12-ARGUS"
    ],
    "binding": "DENY_PENDING_REAL_COLD_EVIDENCE"
  },
  "next_gate": "FIRST_REAL_MODEL_RUNTIME_PASS"
}
