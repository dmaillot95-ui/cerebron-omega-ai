# Signed Acts

This directory stores public, non-secret authorization metadata and verification receipts for critical GUARDIAN acts.

Never commit private keys, seed phrases, recovery codes, API tokens or signing secrets.

Until an AFAH-controlled signing identity is enrolled and independently verified:
CRYPTOGRAPHIC_AFAH_IDENTITY = NOT_ENROLLED
SIGNED_ACT_AUTHORIZATION = HOLD
